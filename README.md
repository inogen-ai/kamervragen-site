# kamervragen.inogen.ai

Static project page for [Kamervragen AI](https://github.com/inogen-ai/kamervragen-ai),
published with GitHub Pages (`.github/workflows/pages.yml`, custom domain in `CNAME`).

The application itself is not hosted here; see the main repository for the
self-hosted stack (`kamervragen serve`, or `deploy/` for Docker Compose with HTTPS).

Built by [Inogen](https://inogen.ai) for SSC-ICT.
